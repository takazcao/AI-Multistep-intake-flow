
  # Design AI Intake Flow

  This is a code bundle for Design AI Intake Flow. The original project is available at https://www.figma.com/design/NJoaCbLC67A1TdtF10qHjT/Design-AI-Intake-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  